# Automatically added by RStudio (ignores the RStudio project file).
^.*\.Rproj$

# Used for temporary files.
^\.Rproj\.user$

# An Rmarkdown file used to generate README.md.
^README\.Rmd$

# Exclude PDF files created by knitting .Rmd files.
#^vignettes/*\.pdf$

# Exclude graphics folder to avoid note when checking package.
^graphics$

# Exclude drafts folder to avoid note when checking package.
^drafts$

# Exclude manuscript folder.
^inst/manuscript$

# Exclude data files. They don't need to be in the package library, just in the 
# local repository. 
data/SAW_Paper_Data.RData$
inst/extdata/Students_Data-WinkeZhang.sav$
inst/extdata/Testlet_Attempts_Data-WinkeZhang.sav$
inst/extdata/164981-V1.zip$
